/** @type {import('tailwindcss').Config} */
export default {
    content: [
      "./index.html",
      "./src/**/*.{js,ts,jsx,tsx}",
    ],
    //darkMode: 'class', // This enables class-based dark mode instead of media query
    theme: {
      extend: {},
    },
    plugins: [],
  }